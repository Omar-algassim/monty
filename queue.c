#include "monty.h"

/**
 * queue - push the elemnt in queue
 * @stack: the head of stack
 * @line_number: the number of the line
 * Return: nothing
 */

void queue(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	info.toggle = 1;

}
